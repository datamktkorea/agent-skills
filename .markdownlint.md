# markdownlint 설정 메모

`.markdownlint.json`에서 비활성화한 규칙과 그 이유.

## 비활성화 규칙

### MD013 — line-length

한국어 문장 + Markdown 링크 문법(`[텍스트](#앵커)`)이 결합되면 80자 제한을 지키는 것이 사실상 불가능하다. 줄을 강제로 끊으면 오히려 가독성이 떨어진다.

### MD028 — no-blanks-blockquote

이 레포의 문서들은 `>` 블록을 *연속된 단일 인용문*이 아니라 **독립된 callout/note 단위**로 사용한다. 블록 사이의 빈 줄은 시각적 분리를 위한 의도적 선택이다.

### MD032 — blanks-around-lists

`>` blockquote 안에서 목록(`> - 항목`)을 바로 이어 쓰는 패턴을 많이 사용한다. 앞뒤에 빈 줄을 요구하면 callout 블록의 흐름이 끊긴다.

### MD036 — no-emphasis-as-heading

`**흐름 누락 점검**`처럼 굵은 글씨를 섹션 내 시각적 소제목으로 쓰는 패턴이 있다. 실제 heading(`####`)으로 올리면 문서 계층 구조가 과도하게 깊어진다.

### MD060 — table-column-style

테이블은 `| 셀 |` 형태의 spaced 스타일을 사용하고, 구분선은 `|---|` compact 스타일을 사용한다. 이 혼용은 가독성을 위한 의도적 선택이며, compact 스타일로 통일하면 셀 내용이 파이프에 붙어 읽기 어려워진다.
